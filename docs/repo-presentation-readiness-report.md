# Repo Presentation Readiness Report

## README Positioning

The README now positions the repo as `Signal Engine`: a deterministic-first signal extraction and evaluation system for long-form business communication.

It keeps earnings-call transcripts as the primary proof domain and frames support, sales, account management, churn risk, and dialogue tone/emotion work as supported/scaffolded/planned rather than validated.

## Remaining Branch / Default Issue

GitHub still reports `main` as the default branch. This remains a manual GitHub action or PR decision.

The public landing README may remain stale until Keith either changes the default branch to `signal-engine-2.0` or merges `signal-engine-2.0` into `main`.

## Branch Count

The local branch count remains high. This report does not delete branches. Branch cleanup should happen only after confirming branches are merged, obsolete, or safely preserved.

## Recommended User Action

1. Set `signal-engine-2.0` as the default branch for the fastest portfolio presentation fix.
2. Or open a PR from `signal-engine-2.0` into `main` if Keith wants `main` to remain default.
3. Review stale branches manually and delete only after confirming they are merged or obsolete.

## No-Claim Boundary

The README does not claim validated ML, statistical significance, production retrieval, market-reaction proof, or validated sales/support/account benchmarks.
