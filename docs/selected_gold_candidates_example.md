# Selected Gold Candidates Example

This is illustrative only. These rows are not approved labels.

Use the CSV template at `docs/selected_gold_candidates_template.csv`, then add rows selected by human review from each case packet.

```csv
case_id,candidate_id,type,confidence,notes
AAPL_2026_Q1,AAPL_2026_Q1_CAND_06,analyst_pressure,high,Illustrative only - human must confirm before use.
```

Only explicit selected candidate IDs should be converted into `gold_labels.jsonl`.
