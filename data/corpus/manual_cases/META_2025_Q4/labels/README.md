# Labels Folder

Purpose: hold reviewer-owned manual labels after source, section, and speaker quality are checked.

Next manual step: create `manual_labels.jsonl` only after transcript sectioning is reviewed. Use `data/gold_labels.example.jsonl` as the schema reference, not as training truth.
